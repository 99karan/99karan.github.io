Put your resume PDF here as:

  karan-prajapat-resume.pdf

The path is configured in src/data/portfolio.ts (profile.resume.file).
Until the file exists, the "Download resume" button will 404 — the in-page
resume preview still works, because it is generated from portfolio.ts.
